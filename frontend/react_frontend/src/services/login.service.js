import ApiService from "./api.service";

const LoginService = {
  login(params) {
    // return ApiService.post("user", { user: { email: params.email, password: params.password } }).then()
  },

};

export default LoginService;
